<li>
	{{name}}
	{{#if eaten}}
	<button class="change-eaten" data-id="{{id}}" data-neweaten="{{eaten}}">
	Consume
	</button>
	{{else}}
	<button class="delete-plate" data-id="{{this.id}}">
	Clear My Plate.
	</button>
	{{/if}}
</li>
